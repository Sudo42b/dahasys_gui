---
phase: 02-service-module-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - minias/serial_comm.py
  - minias/calculator.py
  - minias_app.py
  - minias/__init__.py
autonomous: true
requirements: [STRUCT-04, STRUCT-05]

must_haves:
  truths:
    - "SerialCommunicator class exists in minias/serial_comm.py with identical logic to the original"
    - "SERIAL_AVAILABLE feature flag lives in serial_comm.py alongside the try/except import"
    - "TestCalculator class exists in minias/calculator.py with identical logic to the original"
    - "minias_app.py imports both classes from the new modules instead of defining them locally"
    - "App launches and runs identically after both extractions"
  artifacts:
    - path: "minias/serial_comm.py"
      provides: "SerialCommunicator class + SERIAL_AVAILABLE flag"
      exports: ["SerialCommunicator", "SERIAL_AVAILABLE"]
      min_lines: 200
    - path: "minias/calculator.py"
      provides: "TestCalculator class"
      exports: ["TestCalculator"]
      min_lines: 50
  key_links:
    - from: "minias_app.py"
      to: "minias/serial_comm.py"
      via: "from minias.serial_comm import SerialCommunicator, SERIAL_AVAILABLE"
      pattern: "from minias\\.serial_comm import"
    - from: "minias_app.py"
      to: "minias/calculator.py"
      via: "from minias.calculator import TestCalculator"
      pattern: "from minias\\.calculator import"
---

<objective>
Extract the two leaf-level service classes (SerialCommunicator, TestCalculator) from the monolith into dedicated modules.

Purpose: These are zero-internal-dependency classes (serial_comm uses only stdlib+pyserial, calculator uses only stdlib+models). Extracting them first follows the bottom-up DAG order and removes ~270 lines from the monolith.

Output: `minias/serial_comm.py` and `minias/calculator.py` created; `minias_app.py` updated to import from new modules.
</objective>

<execution_context>
@/home/sw.lee/.config/Claude/get-shit-done/workflows/execute-plan.md
@/home/sw.lee/.config/Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-package-scaffold-leaf-models/01-01-SUMMARY.md
@minias_app.py

<interfaces>
<!-- Current state after Phase 1: minias_app.py imports models from minias.models -->

From minias_app.py line 56:
```python
from minias.models import TestResult, AxisResult, CodeInfo, SetupInfo, LimitInfo
```

From minias/__init__.py (current re-exports — must be extended):
```python
"""MINIAS Probe Testing System"""
__version__ = "1.0.0"
from minias.models import TestResult, AxisResult, CodeInfo, SetupInfo, LimitInfo
```

SerialCommunicator (lines 704-917): Self-contained class using queue, threading, time, re, and pyserial. No references to any minias.* module. Returns Optional[float] from parse methods. SERIAL_AVAILABLE flag at lines 18-25.

TestCalculator (lines 924-981): Pure static-method class using statistics and LimitInfo from models. 5 methods: calculate_sigma, calculate_range, calculate_mean, evaluate_axis_result, evaluate_overall_result.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract SerialCommunicator to minias/serial_comm.py</name>
  <files>minias/serial_comm.py, minias_app.py, minias/__init__.py</files>
  <action>
1. Create `minias/serial_comm.py` with:
   - Module docstring: `"""시리얼 통신 모듈"""`
   - The `SERIAL_AVAILABLE` try/except block (currently at minias_app.py lines 17-25): copy the `try: import serial; import serial.tools.list_ports; SERIAL_AVAILABLE = True; except ImportError: SERIAL_AVAILABLE = False; print(...)` block verbatim
   - Stdlib imports needed by the class: `import queue`, `import threading`, `import time`, `import re` (check class body for all imports used)
   - The `from typing import Optional, List` import for type hints used in the class
   - Copy the entire `SerialCommunicator` class (lines 704-917) VERBATIM — no logic changes, no bug fixes
   - Use absolute imports: `# No internal minias imports — this is a leaf module`

2. In `minias_app.py`:
   - Remove the `SERIAL_AVAILABLE` try/except block (lines 17-25) — it now lives in serial_comm.py
   - Remove the `SerialCommunicator` class definition (lines 699-917, including the banner comment `# === 시리얼 통신 모듈 ===`)
   - Add import: `from minias.serial_comm import SerialCommunicator, SERIAL_AVAILABLE` (in the local imports section, after the existing `from minias.models import ...` line)

3. Update `minias/__init__.py` to add re-export:
   - Add: `from minias.serial_comm import SerialCommunicator, SERIAL_AVAILABLE`

CRITICAL: This is a pure structural extraction. No logic changes, no bug fixes (send_command dead code stays — that's Phase 4). The diff should show only: new file with class, old file with class removed + import added.
  </action>
  <verify>
    <automated>python -c "from minias.serial_comm import SerialCommunicator, SERIAL_AVAILABLE; print(f'serial OK, available={SERIAL_AVAILABLE}')" && python -c "from minias_app import MiniasDatabase; print('monolith OK')"</automated>
  </verify>
  <done>serial_comm.py exists with SerialCommunicator class and SERIAL_AVAILABLE flag. minias_app.py no longer defines SerialCommunicator or SERIAL_AVAILABLE locally. App loads without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Extract TestCalculator to minias/calculator.py</name>
  <files>minias/calculator.py, minias_app.py, minias/__init__.py</files>
  <action>
1. Create `minias/calculator.py` with:
   - Module docstring: `"""테스트 계산 로직"""`
   - Stdlib imports: `import statistics`, `from typing import List`
   - Internal import: `from minias.models import LimitInfo` (TestCalculator.evaluate_axis_result and evaluate_overall_result take LimitInfo parameter)
   - Copy the entire `TestCalculator` class (lines 924-981) VERBATIM

2. In `minias_app.py`:
   - Remove the `TestCalculator` class definition (lines 919-981, including the banner `# === 테스트 계산 로직 ===`)
   - Remove the `import statistics` from the top-level imports IF it's only used by TestCalculator (check if statistics is used elsewhere — it is NOT used elsewhere)
   - Add import: `from minias.calculator import TestCalculator` (in the local imports section)

3. Update `minias/__init__.py` to add re-export:
   - Add: `from minias.calculator import TestCalculator`

CRITICAL: Pure structural extraction. No changes to the static methods or their logic.
  </action>
  <verify>
    <automated>python -c "from minias.calculator import TestCalculator; from minias.models import LimitInfo; tc = TestCalculator(); sigma = tc.calculate_sigma([1.0, 2.0, 3.0]); print(f'calculator OK, sigma={sigma:.4f}')" && python -c "from minias_app import MiniasDatabase; print('monolith OK')"</automated>
  </verify>
  <done>calculator.py exists with TestCalculator class. minias_app.py no longer defines TestCalculator locally. Calculator functions produce identical results. App loads without errors.</done>
</task>

</tasks>

<verification>
1. `minias/serial_comm.py` exists with SerialCommunicator class and SERIAL_AVAILABLE flag
2. `minias/calculator.py` exists with TestCalculator class importing LimitInfo from minias.models
3. `minias_app.py` imports both classes from new modules — no local definitions remain
4. `minias/__init__.py` re-exports SerialCommunicator, SERIAL_AVAILABLE, TestCalculator
5. `uv run python minias_app.py` launches without errors
6. No `import statistics` left in minias_app.py (moved to calculator.py)
7. SERIAL_AVAILABLE try/except block no longer in minias_app.py (moved to serial_comm.py)
</verification>

<success_criteria>
- STRUCT-04: SerialCommunicator extracted to minias/serial_comm.py with SERIAL_AVAILABLE flag ✓
- STRUCT-05: TestCalculator extracted to minias/calculator.py ✓
- Zero behavioral change — app runs identically ✓
</success_criteria>

<output>
After completion, create `.planning/phases/02-service-module-extraction/02-01-SUMMARY.md`
</output>
