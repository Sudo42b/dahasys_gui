---
phase: 02-service-module-extraction
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - minias/excel_export.py
  - minias/certificate.py
  - minias_app.py
  - minias/__init__.py
autonomous: true
requirements: [STRUCT-06, STRUCT-07]

must_haves:
  truths:
    - "ExcelExporter class exists in minias/excel_export.py with identical logic"
    - "EXCEL_AVAILABLE feature flag lives in excel_export.py alongside the try/except import"
    - "CertificateGenerator class exists in minias/certificate.py with identical logic"
    - "PDF_AVAILABLE feature flag lives in certificate.py alongside the try/except import"
    - "minias_app.py imports both classes and feature flags from new modules"
    - "App launches and runs identically — Excel export and PDF generation work as before"
  artifacts:
    - path: "minias/excel_export.py"
      provides: "ExcelExporter class + EXCEL_AVAILABLE flag"
      exports: ["ExcelExporter", "EXCEL_AVAILABLE"]
      min_lines: 60
    - path: "minias/certificate.py"
      provides: "CertificateGenerator class + PDF_AVAILABLE flag"
      exports: ["CertificateGenerator", "PDF_AVAILABLE"]
      min_lines: 300
  key_links:
    - from: "minias/excel_export.py"
      to: "minias/models.py"
      via: "from minias.models import TestResult, AxisResult"
      pattern: "from minias\\.models import"
    - from: "minias/certificate.py"
      to: "minias/models.py"
      via: "from minias.models import TestResult, AxisResult, CodeInfo"
      pattern: "from minias\\.models import"
    - from: "minias_app.py"
      to: "minias/excel_export.py"
      via: "from minias.excel_export import ExcelExporter, EXCEL_AVAILABLE"
      pattern: "from minias\\.excel_export import"
    - from: "minias_app.py"
      to: "minias/certificate.py"
      via: "from minias.certificate import CertificateGenerator, PDF_AVAILABLE"
      pattern: "from minias\\.certificate import"
---

<objective>
Extract the two export/output classes (ExcelExporter, CertificateGenerator) from the monolith into dedicated modules.

Purpose: These are the final two service classes to extract. ExcelExporter is small (~67 lines, uses openpyxl). CertificateGenerator is large (~314 lines, uses reportlab). Both wrap optional dependencies with feature flags. After this plan, only MiniasApp and dialogs remain in the monolith.

Output: `minias/excel_export.py` and `minias/certificate.py` created; `minias_app.py` reduced to ~1600 lines (MiniasApp + dialogs + main).
</objective>

<execution_context>
@/home/sw.lee/.config/Claude/get-shit-done/workflows/execute-plan.md
@/home/sw.lee/.config/Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@minias_app.py

<interfaces>
<!-- After Plans 02-01 and 02-02, serial_comm, calculator, and database are extracted.
     Monolith still contains: ExcelExporter, CertificateGenerator, MiniasApp, LimitsDialog, SettingsDialog -->

ExcelExporter (lines ~988-1054 in original, shifted after prior extractions):
- __init__(self, template_path=None) — stores template_path
- export_result(self, result: TestResult, axis_results: List[AxisResult], output_path: str) -> bool
- Dependencies: openpyxl (optional), models (TestResult, AxisResult)
- EXCEL_AVAILABLE flag at lines 28-34

CertificateGenerator (lines ~1061-1374 in original, shifted after prior extractions):
- __init__(self) — sets up resource paths using os.path
- generate(self, result: TestResult, axis_results: List[AxisResult], code_info: CodeInfo, output_path: str) -> bool
- Dependencies: reportlab (optional), os, traceback, models (TestResult, AxisResult, CodeInfo)
- PDF_AVAILABLE flag at lines 37-53
- Contains duplicate reportlab imports inside generate() (BUGF-03 — leave as-is for Phase 4)
- Uses os.path.dirname(os.path.abspath(__file__)) for resource paths — verify this still resolves correctly from the new minias/ package location
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract ExcelExporter to minias/excel_export.py</name>
  <files>minias/excel_export.py, minias_app.py, minias/__init__.py</files>
  <action>
1. Create `minias/excel_export.py` with:
   - Module docstring: `"""Excel 출력 모듈"""`
   - The `EXCEL_AVAILABLE` try/except block (currently in minias_app.py): copy `try: from openpyxl import Workbook, load_workbook; EXCEL_AVAILABLE = True; except ImportError: ...` verbatim
   - Stdlib imports: `import os` (if used in the class — check), `from typing import List`
   - Internal import: `from minias.models import TestResult, AxisResult`
   - Copy the entire `ExcelExporter` class VERBATIM (including the unused `template_path` parameter — dead code removal is Phase 4, DEAD-04)

2. In `minias_app.py`:
   - Remove the `EXCEL_AVAILABLE` try/except block
   - Remove the `ExcelExporter` class definition (including banner `# === Excel 출력 모듈 ===`)
   - Add import: `from minias.excel_export import ExcelExporter, EXCEL_AVAILABLE` (in local imports section)

3. Update `minias/__init__.py`:
   - Add: `from minias.excel_export import ExcelExporter, EXCEL_AVAILABLE`

CRITICAL: Keep template_path parameter in __init__ — removing it is Phase 4 work (DEAD-04).
  </action>
  <verify>
    <automated>python -c "from minias.excel_export import ExcelExporter, EXCEL_AVAILABLE; print(f'excel OK, available={EXCEL_AVAILABLE}')" && python -c "from minias_app import MiniasApp; print('monolith OK')"</automated>
  </verify>
  <done>excel_export.py exists with ExcelExporter class and EXCEL_AVAILABLE flag. minias_app.py no longer defines ExcelExporter or EXCEL_AVAILABLE. App loads without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Extract CertificateGenerator to minias/certificate.py</name>
  <files>minias/certificate.py, minias_app.py, minias/__init__.py</files>
  <action>
1. Create `minias/certificate.py` with:
   - Module docstring: `"""PDF 인증서 출력 모듈"""`
   - The `PDF_AVAILABLE` try/except block: copy the entire `try: from reportlab.lib.pagesizes import A4; from reportlab.lib import colors; ...` block verbatim
   - Stdlib imports: `import os`, `import traceback`, `from typing import List, Optional`
   - Internal import: `from minias.models import TestResult, AxisResult, CodeInfo`
   - Copy the entire `CertificateGenerator` class VERBATIM

   IMPORTANT: The class uses `os.path.dirname(os.path.abspath(__file__))` in __init__ to find resources. Since the module now lives in `minias/certificate.py` instead of `minias_app.py`, `__file__` will resolve to `minias/certificate.py`. The resource paths `resources/form.xlsx` and `resources/logo.png` are relative to CWD (project root), NOT relative to __file__. Check if the existing code uses `self.script_dir` to construct resource paths — if it does, the path will be wrong from the new location. FIX: Change `self.script_dir` to use CWD instead, OR keep the path construction but verify it still works. Per STATE.md key context #7: "File paths are relative to CWD."

   If the class uses `os.path.join(self.script_dir, 'resources', ...)`:
   - This would break because `self.script_dir` would now be `minias/` instead of the project root
   - Replace `self.script_dir = os.path.dirname(os.path.abspath(__file__))` with `self.script_dir = os.getcwd()` to maintain the CWD-relative path behavior
   - OR simply use hardcoded relative paths like `"resources/form.xlsx"` as the rest of the app does
   - This is NOT a logic change — it's maintaining identical behavior despite file relocation

   CRITICAL: Keep the duplicate reportlab imports inside generate() method — removing them is Phase 4 work (BUGF-03).

2. In `minias_app.py`:
   - Remove the `PDF_AVAILABLE` try/except block
   - Remove the `CertificateGenerator` class definition (including banner `# === PDF 인증서 출력 모듈 ===`)
   - Add import: `from minias.certificate import CertificateGenerator, PDF_AVAILABLE` (in local imports section)

3. Update `minias/__init__.py`:
   - Add: `from minias.certificate import CertificateGenerator, PDF_AVAILABLE`

4. After extraction, verify resource path resolution:
```python
python -c "
from minias.certificate import CertificateGenerator
cg = CertificateGenerator()
print(f'logo_path: {cg.logo_path}')
print(f'form_path: {cg.form_path}')
import os
print(f'logo exists: {os.path.exists(cg.logo_path)}')
print(f'form exists: {os.path.exists(cg.form_path)}')
"
```
If resource paths don't resolve, fix the `self.script_dir` initialization as described above.
  </action>
  <verify>
    <automated>python -c "from minias.certificate import CertificateGenerator, PDF_AVAILABLE; cg = CertificateGenerator(); print(f'certificate OK, available={PDF_AVAILABLE}')" && python -c "from minias_app import MiniasApp; print('monolith OK')"</automated>
  </verify>
  <done>certificate.py exists with CertificateGenerator class and PDF_AVAILABLE flag. Resource paths resolve correctly from the new location. Duplicate reportlab imports preserved (Phase 4 cleanup). minias_app.py no longer defines CertificateGenerator or PDF_AVAILABLE. App loads without errors.</done>
</task>

</tasks>

<verification>
1. `minias/excel_export.py` exists with ExcelExporter class and EXCEL_AVAILABLE flag
2. `minias/certificate.py` exists with CertificateGenerator class and PDF_AVAILABLE flag
3. certificate.py resource paths (logo, form) resolve correctly from CWD
4. All three optional-import try/except blocks (serial, excel, pdf) removed from minias_app.py
5. minias_app.py now contains only: MiniasApp, LimitsDialog, SettingsDialog, main()
6. `minias/__init__.py` re-exports all extracted classes and feature flags
7. `uv run python minias_app.py` launches without errors
</verification>

<success_criteria>
- STRUCT-06: ExcelExporter extracted to minias/excel_export.py with EXCEL_AVAILABLE flag ✓
- STRUCT-07: CertificateGenerator extracted to minias/certificate.py with PDF_AVAILABLE flag ✓
- Zero behavioral change — Excel export and PDF generation work identically ✓
- All 5 service classes now extracted from monolith ✓
- Monolith contains only GUI components (MiniasApp, dialogs, main) — ready for Phase 3 ✓
</success_criteria>

<output>
After completion, create `.planning/phases/02-service-module-extraction/02-03-SUMMARY.md`
</output>
